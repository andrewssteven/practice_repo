#include<stdio.h>
/**
 * main - prints name and ask for correct
 * answer
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int x, a;
	for (x = 1; x <= 03; x++)
	{
	printf("\nDamilola o!");
	}
	printf("\ne me elo ni mo pe e");
	printf("\no le dami lohun abi : ");
	scanf("%d", &a);
	if (a == 3)
{
	printf("\n%dko oju ma ise e o", a);
}
	else
{
	printf("\n%dmo ti mo pe o ni gbo oro", a);
}
return (0);
}
